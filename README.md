# Speed Towing & Recovery — Black/Red Professional Static Site

## Quick edits
1) Replace `https://example.com` in:
- `sitemap.xml`
- canonical tags in HTML (search `example.com`)

2) Set the dispatch inbox:
- `/contact.html` → replace `data-mailto-to="dispatch@yourdomain.com"`

## Deploy (GitHub Pages)
Repo → Settings → Pages → Deploy from branch → main / root.

## Notes
- Optimized to avoid heavy images (SVG hero background).
- Add real photos later in `/assets/img/` if desired.
